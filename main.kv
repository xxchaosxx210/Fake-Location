#:import NoTransition kivy.uix.screenmanager.NoTransition
#:import SearchContent search.SearchContent
#:import LogScreen log.LogScreen
#:include main_map.kv

ScreenManager:
    transition: NoTransition()
    MDScreen:
        name: "mapview"
        MapContainer:
            id: id_map_screen_container
    MDScreen:
        name: "search"
        SearchContent:
    LogScreen:
        name: "log"
